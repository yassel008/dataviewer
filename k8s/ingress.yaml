apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gov-data-viewer-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: gov-data-viewer-service
            port:
              number: 80
